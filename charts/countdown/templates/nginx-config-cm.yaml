{{- if .Values.nginx.customConfig.enabled -}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "countdown.fullname" . }}-nginx-config
  labels:
    {{- include "countdown.labels" . | nindent 4 }}
data:
  default.conf: |
{{ .Values.nginx.customConfig.content | indent 4 }}
{{- end -}}