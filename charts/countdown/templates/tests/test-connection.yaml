apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "countdown.fullname" . }}-test-connection"
  labels:
    {{- include "countdown.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "countdown.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
