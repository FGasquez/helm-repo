apiVersion: v1
kind: Service
metadata:
  name: {{ include "countdown.fullname" . }}
  labels:
    {{- include "countdown.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "countdown.selectorLabels" . | nindent 4 }}
