apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "affine.fullname" . }}
  labels:
    {{- include "affine.labels" . | nindent 4 }}
data:
  REDIS_SERVER_HOST: {{ include "affine.fullname" . }}-redis
  AFFINE_INDEXER_ENABLED: "false"
