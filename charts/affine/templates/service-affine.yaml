apiVersion: v1
kind: Service
metadata:
  name: {{ include "affine.fullname" . }}
  labels:
    {{- include "affine.labels" . | nindent 4 }}
spec:
  type: {{ .Values.affine.service.type }}
  ports:
    - port: {{ .Values.affine.service.port }}
      targetPort: 3010
      protocol: TCP
      name: http
  selector:
    {{- include "affine.selectorLabels" . | nindent 4 }}
